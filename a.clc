class TestHash<K implements IComparable ,V implements IComparable >
{
    static void main() {
        Tuple<K,V> tuple = new Tuple<K,V>();

        K a = new K();
        K b = new K();

        a == b;
    }
}
