class MethodBlockTestClass {
    int field;

    MethodBlockTestClass(int value) {
        self.field = value;
    }

    void method() with void block(int n) {
        for(int i=0; i<5; i++) {
            block(i);
        }
    }
}
