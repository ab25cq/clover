class MyArray<T implements IComparable&IInspectable&IClonable> extends Array<T> {
    MyArray(Array<T> value) {
        self.setValue(value);
    }

    Array<T> method() {
        return self.getValue();
    }
}

class MyString extends String {
    MyString(String value) {
        self.setValue(value);
    }

    String method() {
        return self.getValue();
    }
}
